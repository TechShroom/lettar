/**
 * Add-ons to lettar. These classes are not used by the library. They are
 * intended to serve as a "third-party" library bundled with lettar, that only
 * uses public API and is called by library users. For example, there is an SSE
 * emitter included that provides a {@link com.techshroom.lettar.Response
 * Response<InputStream>} to be copied out.
 */
package com.techshroom.lettar.addons;